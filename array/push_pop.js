const numbers = [12,22,13,14];
const ages=[]

console.log(numbers)
numbers.push(10)//push the eleement to add a new index to end of the previous array
numbers.push(69,20,19,23,59)
console.log(numbers)
numbers.pop()///remove the last element from the array
numbers.pop()
numbers.pop()
console.log(numbers)
const ok = numbers.pop()
console.log('pop value: '+ok)
console.log('after pop the value the new array is: '+numbers)